// Package graph is the node and edge model and the edge weighting rules.
// It is pure data and pure functions over that data.
// No IO, no git, no filesystem, no dependency on any other internal package.
package graph
