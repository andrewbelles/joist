// Package explore holds the advisory path: Leiden community detection,
// scattering and entropy metrics, and smell detectors. Its output is
// seed dependent and is a bootstrapping aid, never a gate.
// Nothing here may be keyed into the artifact or read by the CI check, and
// internal/conform must never import this package.
package explore
