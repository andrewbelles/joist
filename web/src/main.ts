// Entry point. Placeholder only; it renders without touching the API so the
// build path can be verified before any view exists.

const app = document.querySelector<HTMLElement>("#app");

if (app) {
  app.textContent = "joist viewer: no views implemented yet";
}
